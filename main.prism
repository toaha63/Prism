$start = time();
$count = 0;

while true:
{
    $num = rand(0, 2147483640);
     print($num);
    flush();
    parent $count = $count + 1;
    
    $elapsed = time() - $start;
    if $elapsed >= 5:
    {
        break;
    }
}

print("\n--- Results ---");
print("Total numbers printed: " + string($count));
print("Time elapsed: " + string(time() - $start) + " seconds");
